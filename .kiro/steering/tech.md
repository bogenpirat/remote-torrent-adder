---
inclusion: always
---

See `.agents/README.md` for full project documentation.

Build: `npm run build` → `dist/` (dev), `npm run build:prod` → `dist-prod/` (prod). Three Vite builds (popup, options, notifications) + Rollup (service worker + content script). TypeScript strict mode off. No automated tests.
